// Test stub for the `server-only` / `client-only` guard packages, which throw
// when imported outside their intended environment. Under vitest (jsdom) we
// alias the guards here so server modules can be unit-tested directly.
export {}
